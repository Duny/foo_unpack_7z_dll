This is a foobar2000 component for unpacking 7-zip archives, which uses 7z.dll interface for decoding.

Building
In order to build, you will need to define some environment variables:
1. 7ZSDK must point to the directory, where 7z source code can be found
2. FB2SDK must point to the folder with foobar2000 SDK
3. BUILD_OUT_DIR will be used for project's binaries
