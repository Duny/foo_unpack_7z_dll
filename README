This is a foobar2000 component for unpacking 7-zip archives, which uses 7z.dll interface for decoding.
It has several optimizations for intensive archive usage like, for example, having music packed to *.7z files in Media Library.

Building
In order to build, you will need to define some environment variables:
1. SEVENZIPSRC must point to the directory with 7-zip source code
2. FB2KSDK must point to the directory with foobar2000 SDK
3. BOOST for the path to Boost library
BUILD_BIN_DIR will be used for project's binary files (*.obj, etc)
